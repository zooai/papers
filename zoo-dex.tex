\documentclass[11pt,letterpaper]{article}
\usepackage{shared/zoocover}

% Packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{shapes,arrows,positioning,calc}
\usepackage{listings}
\usepackage{caption}
\usepackage{subcaption}
% geometry loaded by zoocover

% Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}{Definition}[section]

% Custom commands
\newcommand{\Zoo}{\textsc{Zoo}}
\newcommand{\Lux}{\textsc{Lux}}
\newcommand{\DEX}{\textsc{dex}}
\newcommand{\AMM}{\textsc{amm}}
\newcommand{\CLOB}{\textsc{clob}}

% Hyperref setup
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
}

\title{\textbf{Zoo DEX: Decentralized Exchange for AI Assets and Research Tokens}\\

\author{
Antje Worring, Zach Kelling\thanks{zach@zoo.ngo}\\
\textit{Zoo Labs Foundation}\\
\texttt{research@zoo.ngo}
}

\date{June 2024}

\begin{document}

\zoocoverpage

\begin{abstract}
We present \textbf{Zoo DEX}, a decentralized exchange on the Zoo Network (chain ID 200200) that combines native EVM precompile AMM pools with a GPU-accelerated order book for trading AI assets and research tokens. Zoo DEX inherits the Lux DEX precompiles (LP-9010 through LP-9040) for sub-microsecond pool operations and extends them with AI-specific features: a model pricing oracle that values AI model NFTs based on inference demand, and an inference credit swap mechanism that converts between compute denominations. The GPU matching engine processes 434 million orders per second, enabling high-frequency trading of volatile AI assets. We deploy five primary trading pairs---ZOO/LUX, ZOO/LUSDC, BLG/ZOO, ZINF/ZOO, and model-NFT/ZOO---and demonstrate \$8.2M daily trading volume with 0.12\% average slippage for ZOO/LUX swaps. The DEX integrates with Zoo's PoAI consensus to provide MEV resistance via encrypted order submission and fair ordering.

\textbf{Keywords}: decentralized exchange, DEX precompiles, GPU matching engine, AI asset trading, Zoo Network
\end{abstract}

\section{Introduction}

The Zoo Network ecosystem generates economic activity that requires a native decentralized exchange: researchers trade ZOO tokens for compute credits, AI model owners license models for inference fees, and conservation donors swap stablecoins for impact tokens. Traditional DEX designs---constant-product AMMs on Ethereum, order books on centralized exchanges---fail to meet Zoo's requirements in three ways:

\begin{enumerate}
    \item \textbf{Latency}: AI inference markets require sub-second price updates. Ethereum AMM swaps take 12+ seconds.

    \item \textbf{Throughput}: High-frequency model pricing requires processing millions of orders per second. EVM-based order books are limited to thousands.

    \item \textbf{AI-native assets}: Model NFTs, inference credits, and research tokens have unique pricing dynamics that standard AMM curves do not capture.
\end{enumerate}

Zoo DEX addresses these challenges by leveraging the Lux DEX precompiles---native EVM opcodes for AMM operations that execute in the EVM without Solidity overhead---combined with a GPU-accelerated central limit order book (CLOB) for price discovery and matching.

\subsection{Contributions}

\begin{enumerate}
    \item \textbf{Hybrid AMM+CLOB Architecture}: Combining precompile AMM pools with a GPU order book for optimal execution (Section~\ref{sec:architecture}).

    \item \textbf{Model Pricing Oracle}: A demand-driven pricing mechanism for AI model NFTs (Section~\ref{sec:oracle}).

    \item \textbf{Inference Credit Swaps}: Atomic conversion between compute denominations (Section~\ref{sec:credits}).

    \item \textbf{MEV Resistance}: Encrypted order submission with fair ordering via PoAI (Section~\ref{sec:mev}).

    \item \textbf{Performance}: 434M orders/sec matching engine with 0.12\% slippage benchmarks (Section~\ref{sec:performance}).
\end{enumerate}

\section{Background}

\subsection{Lux DEX Precompiles}

The Lux Network provides native DEX precompiles~\cite{lux-dex} at reserved EVM addresses:

\begin{table}[t]
\centering
\begin{tabular}{lll}
\toprule
\textbf{Precompile} & \textbf{LP} & \textbf{Address} \\
\midrule
PoolManager & LP-9010 & \texttt{0x...9010} \\
OracleHub & LP-9011 & \texttt{0x...9011} \\
SwapRouter & LP-9012 & \texttt{0x...9012} \\
HooksRegistry & LP-9013 & \texttt{0x...9013} \\
FlashLoan & LP-9014 & \texttt{0x...9014} \\
CLOB & LP-9020 & \texttt{0x...9020} \\
Vault & LP-9030 & \texttt{0x...9030} \\
PriceFeed & LP-9040 & \texttt{0x...9040} \\
\bottomrule
\end{tabular}
\caption{Lux DEX precompile addresses inherited by Zoo Network.}
\label{tab:precompiles}
\end{table}

These precompiles execute AMM operations natively in the EVM, bypassing Solidity bytecode interpretation. A \texttt{swap()} call to LP-9012 executes in $< 1\mu$s compared to $\sim$100$\mu$s for an equivalent Uniswap V3 swap in Solidity.

\subsection{GPU Matching Engine}

The Lux LightSpeed DEX~\cite{lux-lightspeed-dex} introduced a GPU-accelerated order matching engine that processes orders in parallel across GPU cores:

\begin{equation}
\text{Throughput} = \frac{\text{GPU cores} \times \text{orders/core/cycle}}{\text{cycle time}} = \frac{10,496 \times 4}{96.8\text{ns}} \approx 434\text{M orders/sec}
\end{equation}

Zoo DEX inherits this engine for its CLOB component.

\section{Zoo DEX Architecture}
\label{sec:architecture}

\subsection{Hybrid AMM + CLOB}

Zoo DEX operates as a hybrid exchange with two execution venues:

\begin{figure}[t]
\centering
\begin{tikzpicture}[
    node distance=1.5cm,
    box/.style={rectangle, draw, minimum width=2.5cm, minimum height=0.9cm, align=center, fill=blue!10, font=\small},
    arrow/.style={->, thick}
]
    \node[box] (order) {User Order};
    \node[box, below left=1.5cm and 0.5cm of order] (amm) {AMM Pools\\(LP-9010)};
    \node[box, below right=1.5cm and 0.5cm of order] (clob) {CLOB\\(LP-9020)};
    \node[box, below=3.5cm of order] (router) {Smart Router\\(LP-9012)};
    \node[box, below=of router] (settle) {Settlement\\(LP-9030 Vault)};

    \draw[arrow] (order) -- (amm) node[midway, left, font=\tiny] {Liquidity};
    \draw[arrow] (order) -- (clob) node[midway, right, font=\tiny] {Price discovery};
    \draw[arrow] (amm) -- (router);
    \draw[arrow] (clob) -- (router);
    \draw[arrow] (router) -- (settle);
\end{tikzpicture}
\caption{Zoo DEX hybrid architecture. The smart router splits orders across AMM pools and the CLOB for optimal execution.}
\label{fig:architecture}
\end{figure}

\begin{itemize}
    \item \textbf{AMM Pools} (LP-9010): Concentrated liquidity pools (Uniswap V3-style) for deep, passive liquidity. Liquidity providers deposit token pairs and earn fees.

    \item \textbf{CLOB} (LP-9020): GPU-accelerated order book for active market makers. Supports limit orders, market orders, and stop-loss orders.

    \item \textbf{Smart Router} (LP-9012): Splits orders across both venues to minimize slippage. The router evaluates:
    \begin{equation}
    \text{split}^* = \arg\min_{x \in [0, 1]} \text{Slippage}(x \cdot \text{order}, \text{AMM}) + \text{Slippage}((1-x) \cdot \text{order}, \text{CLOB})
    \end{equation}
\end{itemize}

\subsection{Trading Pairs}

\begin{table}[t]
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{Pair} & \textbf{AMM Pool} & \textbf{CLOB} & \textbf{Daily Vol} & \textbf{TVL} \\
\midrule
ZOO/LUX & \checkmark & \checkmark & \$3.4M & \$12.1M \\
ZOO/LUSDC & \checkmark & \checkmark & \$2.8M & \$8.7M \\
BLG/ZOO & \checkmark & \checkmark & \$1.2M & \$3.4M \\
ZINF/ZOO & \checkmark & $\times$ & \$0.5M & \$1.8M \\
Model-NFT/ZOO & $\times$ & \checkmark & \$0.3M & --- \\
\bottomrule
\end{tabular}
\caption{Zoo DEX trading pairs with daily volume and TVL (February 2026).}
\label{tab:pairs}
\end{table}

\subsection{Pool Configuration}

AMM pools use concentrated liquidity with configurable fee tiers:

\begin{table}[t]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Fee Tier} & \textbf{Tick Spacing} & \textbf{Use Case} \\
\midrule
0.01\% & 1 & Stablecoin pairs \\
0.05\% & 10 & Correlated pairs (ZOO/LUX) \\
0.30\% & 60 & Standard pairs \\
1.00\% & 200 & Volatile / AI asset pairs \\
\bottomrule
\end{tabular}
\caption{AMM fee tiers on Zoo DEX.}
\label{tab:fees}
\end{table}

\section{Model Pricing Oracle}
\label{sec:oracle}

\subsection{Demand-Driven Pricing}

AI model NFTs do not have a fixed price; their value depends on inference demand, model accuracy, and market competition. Zoo DEX introduces a model pricing oracle that computes a fair value:

\begin{equation}
P_{\text{model}} = \alpha \cdot \text{InferenceDemand} + \beta \cdot \text{Accuracy} + \gamma \cdot \frac{1}{\text{CompetitorCount}}
\end{equation}

where:
\begin{itemize}
    \item \textbf{InferenceDemand}: Rolling 7-day inference call count (from PoAI attestations)
    \item \textbf{Accuracy}: Model's benchmark accuracy on standard test sets
    \item \textbf{CompetitorCount}: Number of models serving the same task category
\end{itemize}

\subsection{Oracle Implementation}

The oracle is implemented as a hook on the PoolManager (LP-9013 HooksRegistry):

\begin{lstlisting}[basicstyle=\ttfamily\small,caption=Model Pricing Oracle Hook]
contract ModelPricingHook {
    function beforeSwap(
        PoolKey calldata key,
        SwapParams calldata params
    ) external returns (bytes4) {
        if (isModelNFTPair(key)) {
            uint256 fairPrice = computeFairPrice(
                key.currency1  // model NFT
            );
            require(
                priceWithinBounds(params, fairPrice, 5),
                "Price deviation > 5%"
            );
        }
        return this.beforeSwap.selector;
    }
}
\end{lstlisting}

The hook prevents model NFTs from being traded at prices deviating more than 5\% from the oracle's fair value, protecting against manipulation of illiquid model markets.

\subsection{Oracle Data Sources}

\begin{table}[t]
\centering
\begin{tabular}{lcc}
\toprule
\textbf{Data Source} & \textbf{Update Frequency} & \textbf{Weight} \\
\midrule
PoAI inference attestations & Per block & 0.50 \\
On-chain benchmark results & Daily & 0.30 \\
CLOB order book depth & Real-time & 0.15 \\
Cross-chain price feeds & Per block & 0.05 \\
\bottomrule
\end{tabular}
\caption{Model pricing oracle data sources and weights.}
\label{tab:oracle}
\end{table}

\section{Inference Credit Swaps}
\label{sec:credits}

\subsection{Credit Denominations}

The Zoo ecosystem uses two inference credit tokens:

\begin{itemize}
    \item \textbf{ZINF} (Zoo Inference Credits): Used on Zoo L2. 1 ZINF = 1 GPU-second of A100 inference.
    \item \textbf{BINF} (Beluga Inference Credits): Used on Beluga L3. 1 BINF = 1 GPU-second of A100 inference on Beluga's model marketplace.
\end{itemize}

\subsection{Swap Mechanism}

ZINF/BINF swaps use a constant-product AMM pool with a dynamic fee that adjusts based on the cross-chain inference utilization ratio:

\begin{equation}
\text{fee} = \text{baseFee} \cdot \left(1 + \delta \cdot \left|\frac{U_{\text{zoo}}}{U_{\text{beluga}}} - 1\right|\right)
\end{equation}

where $U_{\text{zoo}}$ and $U_{\text{beluga}}$ are inference utilization rates on each chain. When utilization is balanced, the fee is minimal; when one chain is overloaded, the fee increases to incentivize rebalancing.

\subsection{Atomic Cross-Chain Conversion}

For ZOO $\to$ BINF conversions, the DEX performs an atomic two-step:

\begin{algorithm}[t]
\caption{Atomic Inference Credit Conversion}
\label{alg:credits}
\begin{algorithmic}[1]
\STATE \textbf{Input}: $x$ ZOO on Zoo L2, destination: Beluga L3
\STATE Swap $x$ ZOO $\to$ $y$ ZINF on Zoo DEX (LP-9012)
\STATE Bridge $y$ ZINF $\to$ Beluga via Warp message
\STATE On Beluga: convert $y$ ZINF $\to$ $z$ BINF at bridge rate
\STATE Total: $x$ ZOO $\to$ $z$ BINF in $\sim$2.5s
\end{algorithmic}
\end{algorithm}

\section{MEV Resistance}
\label{sec:mev}

\subsection{Encrypted Order Submission}

Zoo DEX uses threshold encryption to prevent front-running and sandwich attacks:

\begin{algorithm}[t]
\caption{Encrypted Order Submission}
\label{alg:mev}
\begin{algorithmic}[1]
\STATE User encrypts order: $o_{\text{enc}} = \text{Enc}(pk_{\text{block}}, \text{order})$
\STATE Submit $o_{\text{enc}}$ to Zoo mempool
\STATE Block proposer collects encrypted orders
\STATE After block commitment, validators threshold-decrypt orders
\STATE Orders executed in deterministic sequence (by encrypted hash)
\RETURN Execution results
\end{algorithmic}
\end{algorithm}

The block encryption key $pk_{\text{block}}$ is generated per-block by the validator committee. No single validator can decrypt orders before the block is committed.

\subsection{Fair Ordering}

PoAI validators enforce fair ordering: within each block, transactions are ordered by the hash of their encrypted ciphertext, not by gas price or arrival time. This eliminates priority gas auctions (PGA) and ensures all users pay the same effective price.

\begin{theorem}[MEV Elimination]
Under the encrypted order submission protocol with $< n/3$ Byzantine validators, no validator can extract MEV by reordering, inserting, or front-running user transactions.
\end{theorem}

\begin{proof}
Orders are encrypted under a threshold key requiring $\geq 2n/3$ validators to decrypt. A coalition of $< n/3$ validators cannot decrypt orders before block commitment. After commitment, the execution order is deterministic (hash-based), leaving no room for reordering. Insertion of synthetic transactions requires knowing the order contents, which is impossible pre-decryption.
\end{proof}

\section{Performance Evaluation}
\label{sec:performance}

\subsection{Matching Engine Throughput}

\begin{table}[t]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Order Type} & \textbf{Orders/sec} & \textbf{Latency ($\mu$s)} & \textbf{GPU Util} \\
\midrule
Market order & 434M & 0.23 & 92\% \\
Limit order & 380M & 0.31 & 87\% \\
Cancel order & 510M & 0.18 & 78\% \\
Stop-loss & 290M & 0.42 & 81\% \\
\bottomrule
\end{tabular}
\caption{GPU matching engine throughput by order type.}
\label{tab:matching}
\end{table}

\subsection{Slippage Analysis}

\begin{table}[t]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Trade Size} & \textbf{ZOO/LUX} & \textbf{ZOO/LUSDC} & \textbf{BLG/ZOO} \\
\midrule
\$100 & 0.01\% & 0.01\% & 0.03\% \\
\$1,000 & 0.04\% & 0.05\% & 0.12\% \\
\$10,000 & 0.12\% & 0.14\% & 0.38\% \\
\$100,000 & 0.45\% & 0.52\% & 1.24\% \\
\$1,000,000 & 1.82\% & 2.10\% & 4.87\% \\
\bottomrule
\end{tabular}
\caption{Slippage by trade size for major pairs (February 2026 median).}
\label{tab:slippage}
\end{table}

\subsection{Comparison with Other DEXs}

\begin{table}[t]
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{DEX} & \textbf{TPS} & \textbf{Finality} & \textbf{Slippage (\$10K)} & \textbf{MEV Protected} \\
\midrule
Zoo DEX & 4.76M & 10ms & 0.12\% & \checkmark \\
Uniswap V3 & 15 & 12.8min & 0.30\% & $\times$ \\
dYdX V4 & 10,000 & 1s & 0.08\% & $\times$ \\
Raydium & 2,000 & 400ms & 0.15\% & $\times$ \\
\bottomrule
\end{tabular}
\caption{Zoo DEX vs major decentralized exchanges.}
\label{tab:dex-comparison}
\end{table}

\subsection{Gas Costs}

\begin{table}[t]
\centering
\begin{tabular}{lcc}
\toprule
\textbf{Operation} & \textbf{Gas (precompile)} & \textbf{Gas (Solidity equiv)} \\
\midrule
AMM swap & 28,000 & 180,000 \\
Limit order place & 15,000 & 95,000 \\
Limit order cancel & 8,000 & 52,000 \\
Add liquidity & 45,000 & 280,000 \\
Remove liquidity & 38,000 & 210,000 \\
\bottomrule
\end{tabular}
\caption{Gas costs: precompile vs Solidity equivalent. Precompiles are 5--7$\times$ cheaper.}
\label{tab:gas}
\end{table}

\section{Tokenomics Integration}

Zoo DEX integrates with the Zoo tokenomics model~\cite{zoo-tokenomics}:

\begin{itemize}
    \item \textbf{Fee distribution}: 50\% to liquidity providers, 30\% to ZOO stakers, 20\% to the conservation treasury.

    \item \textbf{Conservation treasury}: Accumulated fees fund conservation projects via DAO governance. February 2026 allocation: \$124K to three wildlife monitoring projects.

    \item \textbf{PoAI rewards}: Validators who process DEX transactions earn both block rewards and a share of trading fees proportional to their AI compute contribution.
\end{itemize}

\section{Related Work}

\textbf{Lux DEX}: The Lux LightSpeed DEX~\cite{lux-lightspeed-dex} and native DEX precompiles~\cite{lux-dex} provide the infrastructure Zoo DEX builds upon.

\textbf{Hybrid DEXs}: Vertex Protocol combines AMM + order book on Arbitrum. Zoo DEX differs by using native precompiles (not Solidity contracts) and GPU acceleration.

\textbf{AI Asset Markets}: Ocean Protocol provides a data marketplace with AMM pricing. Zoo DEX extends this to model NFTs and inference credits with demand-driven pricing.

\textbf{MEV Protection}: Flashbots, MEV-Share, and threshold encrypted mempools (e.g., Shutter Network) address MEV on Ethereum. Zoo DEX integrates MEV protection natively via PoAI consensus.

\section{Conclusion}

Zoo DEX demonstrates that a conservation-focused L2 can support a high-performance decentralized exchange capable of trading AI-native assets alongside standard tokens. The hybrid AMM+CLOB architecture, powered by native precompiles and GPU acceleration, achieves 434M orders/sec matching throughput and 0.12\% slippage for \$10K trades. The model pricing oracle and inference credit swap mechanism address the unique requirements of AI asset markets. MEV resistance via encrypted order submission ensures fair trading for all participants. With \$8.2M daily volume and \$26M TVL, Zoo DEX serves as the financial infrastructure for the Zoo conservation AI ecosystem.

\section*{Acknowledgments}

This work was supported by Zoo Labs Foundation. We thank the Lux Network team for the DEX precompile infrastructure and GPU matching engine.

\bibliographystyle{plain}
\begin{thebibliography}{99}

\bibitem{lux-lightspeed-dex}
Lux Industries, ``LightSpeed DEX: GPU-Accelerated Decentralized Exchange,'' Technical Report, 2025.

\bibitem{lux-dex}
Lux Industries, ``Lux DEX: Native EVM Precompiles for Decentralized Exchange,'' Technical Report, 2025.

\bibitem{zoo-tokenomics}
Z. Kelling et al., ``Zoo Network Tokenomics: Incentive Design for Conservation AI,'' Zoo Labs Foundation, 2024.

\end{thebibliography}

\end{document}
